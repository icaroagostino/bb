---
title: "Exemplo Bibliometrix"
author: "Icaro Agostino"
date: "27/06/2019"
output: html_document
---

## Exemplo de Análise Bibliométrica

__- Termos Buscados (String)__

Analisar a Literatura sobre __Internet of Things__ e __Costumização em Massa__ utilizando documentos da __WoS__ e __Scopus__, Termos de busca (String):

- TS=(( __"IoT"__ OR __"Internet of Things"__) AND ( __"distributed manufacturing"__ OR __"mass customization"__)) = 57

- TITLE-ABS-KEY-AUTH(( __"IoT"__ OR __"Internet of Things"__) AND ( __"distributed manufacturing"__ OR __"mass customization"__)) = 80

## Carregamento de Dados e Conversão

```{r carrega dados}

library(bibliometrix) # pacote para análise bibliométrica

# Ler bases

b1 <- readFiles("savedrecs.bib") # WoS

b2 <- readFiles("scopus.bib") # Scopus

# Converter pra dataframe

# colocar base "isi" ou "scopus"
# e o formato "bibtex" ou "plaintext"

c1 <- convert2df(b1, dbsource="isi",format="bibtex") # WoS

c2 <- convert2df(b2, dbsource="scopus",format="bibtex") # Scopus

# Unir e remover duplicatas

M <- mergeDbSources(c1, c2, remove.duplicated=TRUE)
```

### Principais características (Análise Descritiva)

```{r}
results <- biblioAnalysis(M)
summary(results, k=10, pause=F, width=130)
```

### Gráficos (Análise Descritiva)

```{r fig.height=4, fig.width=8}
plot(x=results, k=10, pause=F)
```

### Rede de cocitação

```{r fig.height=12, fig.width=12}
NetMatrix <- biblioNetwork(M, analysis = "co-citation",
                           network = "references", sep = ";")

net=networkPlot(NetMatrix, n = 30, Title = "Co-Citation Network",
                type = "fruchterman", size.cex=TRUE, size=20,
                remove.multiple=FALSE, labelsize=0.7,edgesize = 5,
                edges.min=2)
```

### Historiograph - Direct citation linkages

```{r hist}
histResults <- histNetwork(M, min.citations=quantile(M$TC,0.3), sep = ";")

options(width = 130)

net <- histPlot(histResults, n=50, size = 5,
                labelsize = 3)
```

### Estrutura conceitual

```{r fig.height=6, fig.width=8}
CS <- conceptualStructure(M, method="CA", field="ID",
                          minDegree=4, k.max = 8,
                          stemming=f, labelsize=8,documents=20)
```
